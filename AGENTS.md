# agents.md

本文件为项目 AI 助手与人类开发者共同遵守的代码修改纪律。所有操作（包括代码编写、重构、测试、文档更新）均需遵循以下规则，以确保协作高效、质量可靠。

---

## 核心纪律

### 1. 公开 API 变更须经讨论
- **任何对使用者公开的 API、接口和约定（包括但不限于公共函数签名、结构体/枚举字段、trait 定义、模块导出路径、配置文件格式、环境变量名称）的增加、修改或删除，** 在实施前必须获得团队明确同意。  
- 若 AI 助手提出此类变更，应先在对话中说明理由并等待确认，不得自行决策。

### 2. 测试需包含中文文档注释
- 所有测试函数（`#[test]`、`#[tokio::test]` 等）必须使用 **中文** 编写文档注释（`///` 或 `//!`），内容须明确：
  - **测试目标**：该测试验证什么功能或特性。
  - **测试手段**：如何构造输入、模拟依赖、执行被测代码。
  - **判定标准**：通过/失败的具体判断条件（如断言内容）与测试逻辑之间的关联。  
- 示例：
  ```rust
  /// 测试解析器正确处理有效 JSON 字符串。
  /// - 手段：使用 `serde_json::from_str` 解析预定义的合法 JSON 数据。
  /// - 判断：返回的 `Result` 应为 `Ok`，且解析出的结构体字段值与预期完全匹配。
  ```
- 此项要求同样适用于集成测试（`tests/` 目录）和文档测试（`# Examples` 中的代码块）。

### 3. 修改代码前检查未提交变更
- 在开始任何代码改动前，**必须**执行 `git status` 检查工作区是否有未提交的代码文件变更。  
- 若存在未提交变更，应先提醒用户：“检测到未提交的代码变更，建议先提交或暂存后再继续修改。” **在用户明确处理前，不得继续编辑代码文件。**

---

## 补充纪律（针对 Rust 项目与人机协同）

### 4. 对外公开的异步方法的代码约定
- 除非依赖链条上不存在 `gen_mcf_macro`，否则本项目内所有对外公开的异步方法，都应该优先考虑使用 `gen_may_cancel_future` 宏来封装
- 除非依赖链条上不存在 `gen_mcf_macro`，否则所有异步方法的实现，不能为了设计方便，总是假定调用者不会通过取消令牌来发送主动取消信号。

### 5. 命名规范
- 所有由 agent 编写的 struct 成员字段名，若不带有任何 pub 修饰，都以 `_` 作为结尾。
- 所有由 agent 编写的的函数名，若不带有任何 pub 修饰，都以 `_` 作为结尾。
- 函数内的变量，由于默认不能对外公开，应该按照正常的命名规则。

### 6. 遵循 Rust 惯用风格与工具链
- **错误传播**：优先使用 `?` 运算符，避免在库代码中使用 `unwrap()`、`expect()`（仅测试或快速原型除外）。  
- **命名规范**：遵循 Rust 约定（函数/变量用 `snake_case`，类型用 `CamelCase`，常量用 `SCREAMING_SNAKE_CASE`）。  
- **自动格式化**：所有提交的代码必须通过 `cargo fmt` 格式化。  
- **静态检查**：确保 `cargo check` 无警告，并通过 `cargo clippy` 的建议（如有合理例外需注释说明）。

### 7. 谨慎对待 `unsafe` 代码
- 禁止无理由引入 `unsafe`。若确需使用，必须附加详细的安全注释，论证其正确性（如不变量、生命周期、对齐等），并确保所有 `unsafe` 操作均被安全函数封装。

### 8. 公共 API 文档
- 所有公共项（函数、结构体、枚举、trait、宏）必须附带 `///` 文档注释，并包含 `# Examples`、`# Panics`、`# Errors` 等必要章节（如适用）。示例代码须保证编译通过（可通过 `cargo test --doc` 验证）。
- 所有文档说明及注释，正文行文部分应该以中文为主体。

### 9. 错误类型设计
- 自定义错误类型需实现 `std::error::Error` 或者 `core::error::Error`（no_std 的情况），并派生 `Debug`，同时提供清晰的 `Display` 输出，便于用户理解错误上下文。

### 10. 测试层次完整性
- 除单元测试外，应编写集成测试（`tests_/` 目录）覆盖多模块交互场景。对于关键路径，鼓励使用基准测试（`benches/`）或性能回归测试。

---

**生效范围**：本规则适用于本仓库所有代码的编写与修改，AI 助手与人类开发者均须遵守。如有未尽事宜，以团队实际讨论结果为准。
