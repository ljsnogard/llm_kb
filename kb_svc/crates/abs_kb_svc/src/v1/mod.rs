//! 协议 v1。
//!
//! 版本号在 [`desktop::PROTOCOL_VERSION`] 中给出。握手时双方交换该值，
//! 不一致时应当明确拒绝，而不是"尽力而为"地继续。
//!
//! 每个模块描述**一类对端**与 `kb_core` 之间的通信内容：
//!
//! | 模块 | 对端 |
//! | :--- | :--- |
//! | [`desktop`] | 桌面客户端 `kb_admin_desktop` |
//!
//! 插件（`kb_plugins/*`）与 `kb_core` 的通信内容是另一组消息，将在后续以
//! `v1::plugin` 之类的形式加入；两者**不共用**消息类型，因为语义完全不同
//! （客户端关心交互，插件关心 provider 调用）。

pub mod desktop;
